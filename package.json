{
  "name": "favicon-local",
  "version": "1.0.0",
  "description": "Self-hosted favicon service for local network and public domains",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "hono": "^4.7.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
